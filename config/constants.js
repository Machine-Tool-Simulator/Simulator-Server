const ROUTES = {
	INDEX: '/',
	LATHE: '/lathe',
	DEPLOY: '/deploy',
};

const COLLECTION = {
	STATUS: 'status',
	MODULES: 'modules',
};

const TEXT = {
	DF_TITLE: 'Welcome to Dartmouth Machine Shop Simulation',
	DF_DESCRIPTION: 'Watch each video, read the description, and complete any tasks written in bold. After completing the task, click the Submit button to continue. If there is no task written in bold, you can click the Submit button to continue anytime. You will be asked to complete these tasks on the actual machine.',
};

const CONSTANTS = {
	ROUTES: ROUTES,
	COLLECTION: COLLECTION,
	TEXT: TEXT,
};

module.exports = CONSTANTS;
