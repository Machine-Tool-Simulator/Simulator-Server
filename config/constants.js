const ROUTES = {
	INDEX: '/',
	LATHE: '/lathe',
	DEPLOY: '/deploy',
};

const COLLECTION = {
	STATUS: 'status',
	MODULES: 'modules',
};

const CONSTANTS = {
	ROUTES: ROUTES,
	COLLECTION: COLLECTION,
};

module.exports = CONSTANTS;
